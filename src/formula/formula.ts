import * as models from '../models';

/**
 * Represent a formula, which can be read from a file or to be created from the config.
 */
class Formula {
  formula: models.Formula.Formula;

  constructor() {
    // this.formula.name;
  }
}

export default Formula;
